-- lists all records of table `second_table` of database `hbtn_0c_0`
-- display both score and name (in this order) and ordered by score (top first)
SELECT `score`, `name` FROM `second_table` ORDER BY `score` DESC;
